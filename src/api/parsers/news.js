export default (data) => {
    return {
        id: data['id'],
        title: data['title'],
        text: data['text'],
        // title: data['category_id'],
        // title: data['title'],
        // title: data['title'],
        // title: data['title'],
        // title: data['title'],
    }
};